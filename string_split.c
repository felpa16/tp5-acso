#include "string_split.h"

char** split_string(const char* string, char divisor) {
    if (string == NULL) return -1;
    int initial_n_of_strings = 8;
    int string_counter = 0;
    char** result = (char**) malloc(initial_n_of_strings * sizeof(char*));

    int = initial_string_len = 2048;
    char* current_string = (char*) malloc(initial_string_len * sizeof(char));
    for (i )
}